---
title: "{golem} 0.4.0 is now available"
author: colin
date: '2023-03-15'
slug: golem-0.4.0-release-on-cran
---

```{r include = FALSE}
if (!requireNamespace("emo")){
  if (!requireNamespace("remotes")){
    install.packages("remotes")
  }
  remotes::install_github("hadley/emo")
}
if (!requireNamespace("shiny")){
  if (!requireNamespace("remotes")){
    install.packages("remotes")
  }
  remotes::install_cran("shiny")
}
```

![](featured.png)

```{r include = FALSE}
pkg <- yaml::read_yaml(here::here("packages/packages.yaml"))$package
all_cran_paks <- available.packages() |> as.data.frame()
length(which(names(pkg) %in% all_cran_paks$Package))
```

This is a semi automatic blogpost about the state of the golemverse in April 2023.

As of now, `r length(which(names(pkg) %in% all_cran_paks$Package))` packages are on the CRAN.

Here is the

